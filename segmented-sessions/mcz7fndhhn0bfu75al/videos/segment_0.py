from manim import *

class EducationalAnimation(Scene):
    def construct(self):
        title = Text("Understanding Fractions", font_size=72)
        subtitl, e=Text("A Fraction is a Part of a Whole", font_size=36)
        circl, e=Circle(radiu, s=0.5)
        self.play(Create(circle)))
        self.wait(3.0)

        self.play(FadeOut(title), FadeOut(fraction_example))
        self.wait(0.0)