from manim import *

class GraphTheoryAnimation(Scene):
    def construct(self):
        circle = Circle(radius=0.5)
        self.play(Create(circle))
        self.wait(2)

        circle = Circle(radius=0.5)
        self.play(Create(circle))
        self.wait(2)

        circle = Circle(radius=0.5)
        self.play(Create(circle))
        self.wait(2)

        circle = Circle(radius=0.5)
        self.play(Create(circle))
        self.wait(2)

        circle = Circle(radius=0.5)
        self.play(Create(circle))
        self.wait(2)

        circle = Circle(radius=0.5)
        self.play(Create(circle))
        self.wait(2)

        circle = Circle(radius=0.5)
        self.play(Create(circle))
        self.wait(2)

        circle = Circle(radius=0.5)
        self.play(Create(circle))
        self.wait(2)

        circle = Circle(radius=0.5)
        self.play(Create(circle))
        self.wait(2)

        circle = Circle(radius=0.5)
        self.play(Create(circle))
        self.wait(2)

        circle = Circle(radius=0.5)
        self.play(Create(circle))
        self.wait(2)

        circle = Circle(radius=0.5)
        self.play(Create(circle))
        self.wait(2)

        self.wait(2)